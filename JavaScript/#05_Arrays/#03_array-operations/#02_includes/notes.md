# ARRAYS OPERATIONS - INCLUDES

Checking whether a value exists in an array is really easy in JavaScript, just use the `.includes()` method.

    fruits = ['apple', 'banana', 'orange']
    console.log(fruits.includes('orange'))
    // Prints: true
